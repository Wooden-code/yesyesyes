components中 coin，enemy，player
每一个关卡图可以powerpoint搭建

每一个部分先搭框架用pass使它能运行就可以，然后在写具体语句，完善细节

写仔细，有时候一个文件里面函数方法写错，另一边调用没有用，但又不报错
json是一种轻松简洁的数据格式

s+<-或->冲刺
__init__方法必须把后面的都初始化，要不然相当于没有调用，报错 AttributeError:’xxx‘object has no attribute 'xxx'

与源代码不同 :跳跃选择的是向上箭头

游戏目前有的bug：按起跳（向上键）后它即